/** @type {import("next").NextConfig} */

const config = {
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
  i18n: {
    locales: ["en"],
    defaultLocale: "en",
  },
};


export default config;